[package]
name = "riscv"
version = "0.1.0"
description = "RISC-V Architecture specific components."
authors = ["Justus K <justus.k@protonmail.com>"]
edition = "2018"

[dependencies]
spinning_top = { version = "0.2", features = ["nightly"] }
